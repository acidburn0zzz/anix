pub fn fsmain(){
    /*unsafe{
        asm!(r#"
        mov 5, %eax
        mov 1, %ebx
        mov msg, %ecx
        mov 12, %edx
        syscall

        mov 1, %eax
        mov 0, %ebx
        syscall
        "#);
    }*/
}
